<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/*
| Tabel minimal document-builder. Package hanya membaca dua kolom lewat
| IsTemplateRecord: "schema" (JSON) dan "name" (judul di toolbar builder).
| Kolom lain — cabang, tenant, pembuat, status aktif — milik aplikasi Anda;
| tambahkan di sini sebelum menjalankan migrate.
*/
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('document_templates', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->json('schema');
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('document_templates');
    }
};
